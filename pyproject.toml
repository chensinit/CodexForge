[project]
name = "autodev-loop"
version = "0.1.0"
description = "OpenAI-driven Codex CLI development loop"
requires-python = ">=3.12"

[project.scripts]
autodev = "autodev.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
